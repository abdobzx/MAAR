# Requirements simplifiés pour test Docker - résolution des conflits
# Version minimale pour validation du build

# Core Framework (versions stables testées)
fastapi==0.108.0
uvicorn[standard]==0.25.0
pydantic==2.5.3
pydantic-settings==2.1.0

# HTTP Client (compatible avec ollama)
httpx>=0.27.0,<0.29.0

# Database essentiels
sqlalchemy==2.0.25
asyncpg==0.29.0

# Providers AI critiques (versions compatibles)
ollama==0.5.1
cohere>=4.39.0

# Vector Database
qdrant-client==1.7.0

# Utilities de base
aiofiles==23.2.1
python-dotenv==1.0.0
python-multipart==0.0.6

# Security minimal
cryptography>=41.0.0

# Monitoring minimal
prometheus-client==0.19.0
